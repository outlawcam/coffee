node_modules
dist
.DS_Store
incoming
# Subagent-driven-development scratch (ledgers, briefs, review packages)
.superpowers/
# Excel lock files, created while a workbook is open
~$*
# Type collections dropped in for reference. This design is Montserrat-only —
# both the Big Type collection and TAYFlapjack are unused, so the whole
# directory is ignored rather than just the collection subfolder.
src/fonts/
